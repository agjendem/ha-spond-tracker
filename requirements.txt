# Runtime dependencies for the spond_tracker AppDaemon app.
#
# These must be installed inside the AppDaemon runtime — either by adding
# them to the AppDaemon add-on's `python_packages` configuration in
# Home Assistant, or by installing them inside your AppDaemon container
# manually.
#
# Pinned to known-good versions tested with this release. To upgrade,
# bump the version here, the README, info.md, and add a CHANGELOG entry.

spond==1.2.1
